import 'package:flutter/material.dart';

class Login {
  final String username, password, token, name, lastname;
  final int id;

  Login({
    required this.id,
    required this.username,
    required this.password,
    required this.token,
    required this.name,
    required this.lastname,
  });

}